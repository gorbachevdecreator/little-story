# 1. Берем легкую версию Python 3.11
FROM python:3.11-slim

# 2. Создаем папку внутри контейнера, где будет лежать код
WORKDIR /app

# 3. Копируем файл с зависимостями и устанавливаем их
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 4. Копируем весь остальной код проекта в контейнер
COPY . .

# 5. Команда, которая запустит сервер (доступен извне на порту 8000)
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]